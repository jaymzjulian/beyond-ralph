name: beyond-ralph-stop
description: Keep Beyond Ralph running until project is complete
event: Stop

# Run Python stop hook that checks state, quota, and blocks if incomplete
command: python3 .claude/hooks/stop_hook.py

# BLOCKING - this is what makes it continue!
blocking: true
