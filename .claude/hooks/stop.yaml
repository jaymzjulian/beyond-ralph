name: beyond-ralph-stop
description: Save Beyond Ralph orchestrator state when session ends
event: Stop

# Run Python script to save state
command: python -c "from beyond_ralph.hooks.stop_handler import handle_stop; handle_stop()"

# Don't block session exit
blocking: false
